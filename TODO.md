# TODO

## 🎯 マイルストーン
- [x] Phase 1: 基本画面とコピー機能の実装
- [x] Phase 2: 2ペイン化の実装
- [ ] Phase 3: ファイル・ディレクトリの基本操作 (xyzzyファイラ基本機能)
- [ ] Phase 4: マーク・選択・ソート・絞り込み
- [ ] Phase 5: 2ペイン間連携・プレビュー・安全性強化
- [ ] Phase 6: xyzzy風キーバインド・ミニバッファ・操作性最適化
- [ ] Phase 7: 外部連携・カスタマイズ機能
- [ ] Phase 8: パフォーマンス改善・テスト・CI/CD・配布整備

---

## 📋 タスク詳細

### Phase 1: 基本画面とコピー機能の実装
- [x] 基本画面の実装
- [x] コピーボタン

### Phase 2: 2ペイン化の実装
- [x] 2ペイン表示
- [ ] アクティブペインの切り替え (`Tab` / `C-x o` / クリック)
- [ ] アクティブペインおよびフォーカスカーソルの視覚的ハイライト表示

### Phase 3: ファイル・ディレクトリの基本操作 (xyzzyファイラ基本機能)
- [ ] **ディレクトリ移動・ナビゲーション**
  - [ ] ディレクトリに入る / ファイルを開く (`Enter` / `f` / `l` / ダブルクリック)
  - [ ] 親ディレクトリへ移動 (`Backspace` / `u` / `h` / `^`)
  - [ ] パス直接入力移動 (ロケーションバー / `g` または `M-g`)
  - [ ] ドライブ切り替え (`d` / `M-d` : Windowsドライブレター選択)
  - [ ] ファイル一覧の再読み込み (`g` / `F5` / `C-l`)
- [ ] **ファイル・ディレクトリの作成**
  - [ ] 新規ディレクトリ作成 (`k` / `+` / `mkdir`)
  - [ ] 新規空ファイル作成 (`C-x C-f` / `touch`)
- [ ] **ファイル・ディレクトリの編集・削除**
  - [ ] リネーム (名前変更) (`r` / `R` / `F2`)
  - [ ] 削除 (ゴミ箱へ移動 / 完全削除) (`d` / `Delete`)
  - [ ] 関連付けられた外部アプリで開く / 実行 (`Enter` / `x`)
  - [ ] エディタで開く (`e`)

### Phase 4: マーク・選択・ソート・絞り込み
- [ ] **マーク・複数選択 (xyzzyファイラ準拠)**
  - [ ] 単一ファイルマーク・トグル (`Space` でマークしてカーソル下移動)
  - [ ] 単一ファイルマーク解除 (`u` で解除してカーソル下移動)
  - [ ] 全マーク (`* *` / `Ctrl+A`)
  - [ ] 全マーク解除 (`* u` / `U`)
  - [ ] マーク反転 (`* t`)
  - [ ] ワイルドカード / 正規表現によるパターンマーク (`* s`)
- [ ] **表示制御・ソート**
  - [ ] 隠しファイル / 隠しディレクトリの表示切り替え (`.` / `C-x .`)
  - [ ] ソート順の切り替え (`s`: 名前 / 拡張子 / サイズ / 更新日時 / 昇順・降順)
  - [ ] ファイル情報・詳細属性表示のトグル (`i`)
- [ ] **検索・履歴・ブックマーク**
  - [ ] インクリメンタル絞り込み・検索 (`/` / `C-s`)
  - [ ] ディレクトリ移動履歴 (戻る / 進む: `Alt+Left` / `Alt+Right` / `C-c <` / `C-c >`)
  - [ ] ブックマーク / お気に入りディレクトリ登録 (`b` / `M-b`)

### Phase 5: 2ペイン間連携・プレビュー・安全性強化
- [ ] **2ペイン間連携 (マーク対象または選択ファイルの一括処理)**
  - [ ] 反対側ペインへのコピー (`c` / `C`)
  - [ ] 反対側ペインへの移動 (`m` / `M`)
  - [ ] 反対側ペインと同じディレクトリに同期 (`O` / `C-x 4`)
- [ ] **プレビュー機能**
  - [ ] クイックプレビュー表示 (`v` / `Space` プレビューモード)
  - [ ] テキストファイルプレビュー (文字コード自動判別・シンタックスハイライト)
  - [ ] 画像プレビュー表示
- [ ] **安全性・操作性**
  - [ ] 破壊的操作前の確認プロンプト (ミニバッファ / ダイアログ: `y/n`)
  - [ ] エラーハンドリングとメッセージ通知 (ミニバッファ / エコーエリア)
  - [ ] 長時間ファイルI/O処理の非同期実行 (Rust側バックグラウンド処理 + 進捗バー表示 + `C-g` による中断)
  - [ ] 操作ログ表示

### Phase 6: xyzzy風キーバインド・ミニバッファ・操作性最適化
- [ ] **カーソル移動・バッファ操作 (xyzzy/Emacs準拠)**
  - [ ] 上下移動 (`C-p` / `k` / `Up`, `C-n` / `j` / `Down`)
  - [ ] ページスクロール (`PageUp` / `M-v`, `PageDown` / `C-v`)
  - [ ] リストの先頭 / 末尾への移動 (`Home` / `M-<`, `End` / `M->`)
  - [ ] ペイン切り替え (`Tab` / `C-x o` / `C-i`)
  - [ ] ファイラ終了・ウィンドウ閉じる (`q` / `C-x C-c`)
- [ ] **ミニバッファ / コマンドライン / プレフィクスキー**
  - [ ] プレフィクスキー (`C-x`, `C-c`, `*`) のキーシーケンス入力ハンドラ
  - [ ] 汎用キャンセル処理 (`C-g` / `Esc`)
  - [ ] ミニバッファ入力 (`M-x` コマンドパレット実行)
  - [ ] パス補完・コマンド名補完 (`Tab`)
  - [ ] ミニバッファ内での履歴参照 (`M-p` / `M-n` / 上下矢印)
- [ ] **キーバインドのカスタマイズ機構**
  - [ ] キーマップ定義の分離 (`src/keybindings/keymap.ts`)
  - [ ] 設定ファイル経由のユーザーキーマップ上書き対応

### Phase 7: 外部連携・カスタマイズ機能
- [ ] **外部連携**
  - [ ] カレントディレクトリでターミナルを開く (`!` / `M-!`)
  - [ ] 外部エディタ (xyzzy / VSCode等) で開く
  - [ ] 選択ファイルに対する外部コマンド実行 (`X` / `&`)
- [ ] **設定と外観**
  - [ ] 設定ファイル管理 (Rust側で `config.json` または `config.toml` の永続化)
  - [ ] xyzzyクラシック風カラーテーマ / フォント設定
- [ ] **クロスプラットフォーム対応**
  - [ ] Windows (ドライブレター `C:\`、UNCパス) / Linux / macOS パス表記対応
  - [ ] 文字エンコーディング対応 (CP932, UTF-8)

### Phase 8: パフォーマンス改善・テスト・CI/CD・配布整備
- [ ] **パフォーマンス改善**
  - [ ] 大量ファイル表示時の仮想スクロール実装 (React Virtualized / TanStack Virtual)
  - [ ] Rust側の高速ディレクトリスキャン (非同期ディレクトリ走査)
- [ ] **テスト・品質担保**
  - [ ] フロントエンド単体テスト (Vitest / React Testing Library: キーバインド処理、UIコンポーネント)
  - [ ] バックエンド単体・統合テスト (cargo test: Rust側のファイルI/O・パス解決)
- [ ] **ドキュメント・ビルド・配布**
  - [ ] README.md の整備 (インストール手順、xyzzy風キーマップ一覧表)
  - [ ] GitHub Actions CI (cargo clippy, cargo test, npm run lint, npm test)
  - [ ] Tauri Bundler によるインストーラ/単一バイナリ生成 (Windows: MSI/NSIS, Linux: deb/AppImage, macOS: dmg)
